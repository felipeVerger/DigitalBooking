import React from 'react'
import {
    FormContainer,
    FormTitle,
} from './FormStyle'

const Form = () => {
  return (
    <FormContainer>
        <FormTitle>Completa tus datos</FormTitle>
    </FormContainer>
  )
}

export default Form